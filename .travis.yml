language: node_js
node_js:
  - lts/*
cache: npm
install:
  - npm i react-scripts
  - yarn install
before_script:
  - bash <(curl -Ls https://coverage.codacy.com/get.sh) download
script:
  - npm test
after_success:
  - bash <(curl -Ls https://coverage.codacy.com/get.sh)
branches:
  only:
  - main
  - develop
  - B#10